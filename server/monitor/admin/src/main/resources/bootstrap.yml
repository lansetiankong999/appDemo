server:
  port: ${SERVER_PORT:8022}
spring:
  application:
    name: admin
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:47.107.177.14}:${REGISTER_PORT:8848}
      config:
        server-addr: ${REGISTER_HOST:47.107.177.14}:${REGISTER_PORT:8848}
        file-extension: yml
